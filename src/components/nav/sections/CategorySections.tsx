import CoreCreateButton from '@/components/core/CoreCreateButton';

function CategorySections() {
  return (
    <div>
      <CoreCreateButton text='Категори үүсгэх' pathname='/category/create' />
    </div>
  );
}

export default CategorySections;
